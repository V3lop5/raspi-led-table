# LED Matrix table powered by Raspberry Pi


## Installation
Download [Python 3.7](https://www.python.org/downloads/) and install it.

Run the following command to install the required packages:
````bash
pip install -r requirements.txt
````